#1.1 impliment a recursive function to calculate the factorial of a given number.def factorial(n):
    return 1 if n == 0 else n * factorial(n - 1)

number = 5

result = factorial(number)
print(f"The factorial of {number} is {result}")
